@echo off
echo This will update your Firestore security rules using Firebase CLI
echo.
echo First, make sure you have Node.js installed
echo Then run: npm install -g firebase-tools
echo.
echo After that, run these commands:
echo.
echo firebase login
echo firebase use sales-project-tracker
echo firebase deploy --only firestore:rules
echo.
pause